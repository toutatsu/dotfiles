# PR レビュー: ユーザー認証機能の追加

**PR番号:** #42
**ブランチ:** feature/auth → main
**レビュー日:** 2026-04-28

---

## 🔴 重大（要修正）

| ファイル | 行 | 問題 | 提案 |
|---------|-----|------|------|
| `src/auth/login.ts` | L38 | パスワードを平文で `console.log` に出力している | ログ出力を削除するか、`***` でマスクする |
| `src/db/query.ts` | L12 | テンプレートリテラルで SQL を組み立てており SQLインジェクションの危険 | `?` プレースホルダーのプリペアドステートメントを使用 |

---

## 🟡 注意（推奨修正）

| ファイル | 行 | 問題 | 提案 |
|---------|-----|------|------|
| `src/auth/token.ts` | L5 | JWT の有効期限が 30 日と長すぎる | セキュリティポリシーに合わせて短縮（例: 1 時間）し、リフレッシュトークンを導入 |
| `src/middleware/auth.ts` | L22 | エラーメッセージに内部スタックトレースが含まれる | ユーザー向けには汎用メッセージを返し、サーバー側にのみ詳細ログを残す |

---

## 🟢 軽微（任意）

| ファイル | 行 | 問題 | 提案 |
|---------|-----|------|------|
| `src/auth/login.ts` | L1-60 | 関数が 60 行超で長い | `validateCredentials` と `createSession` に分割すると可読性が上がる |

---

## 総評

認証の基本フローは正しく実装されていますが、SQLインジェクションとパスワードのログ出力は本番投入前に必ず修正が必要です。JWT の有効期限も要検討。

**承認:** ⚠️ 要修正
